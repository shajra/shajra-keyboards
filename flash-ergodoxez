#!/bin/sh -eu

cd "$(dirname "$0")"

nix run \
    --ignore-environment \
    --file . \
    shajra-keyboards-flash-scripts.ergodoxez \
    --command \
    flash-ergodoxez \
    --nix "$(command -v nix)" \
    "$@"
